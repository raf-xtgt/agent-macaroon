"""Stub adapter: generic HTTP endpoint for non-ADK frameworks (LangChain, AWS Strands, etc.) — proves the core is framework-agnostic; selected via GATEWAY_MODE=proxy, never exercised in the demo."""

# TODO: implemented in a later milestone — see AGENTS.md

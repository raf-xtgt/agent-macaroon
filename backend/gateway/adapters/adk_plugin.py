"""Real adapter: ADK Plugin (BasePlugin) — wires policy.py into before_tool_callback / before_agent_callback / on_user_message_callback / after_tool_callback at the App level, so the Gateway attaches to any ADK app (own chain or travel-concierge) without modifying its agents."""

# TODO: implemented in a later milestone — see AGENTS.md
